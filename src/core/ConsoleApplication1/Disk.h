#pragma once
class Disk
{
};

// Can use to traverse file system, read/write/copy/delete files, etc
// A top-level "File" class, with subclasses for DLL, script (powershell, python, etc), etc. Some are injectable.